<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    MinWidth="300"
    MinHeight="100"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Margin="3" Text="{Binding User.ID}" />
        <ListBox
            Grid.Row="1"
            Margin="3"
            ItemsSource="{Binding SomeData}" />
        <ListBox
            Grid.Row="2"
            Margin="3"
            ItemsSource="{Binding AnotherData}" />
    </Grid>
</UserControl>